import React from "react";
import { UploadModalType } from "./index.interface";

const UploadModal: React.FC<UploadModalType> = ({ handleClose }) => {
  return (
    <div>
      <p>This is a Upload Modal</p>
    </div>
  );
};

export default UploadModal;
